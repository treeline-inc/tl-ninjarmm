# coding: utf-8

"""
NinjaOne Public API 2.0

NinjaOne Public API documentation.

The version of the OpenAPI document: 2.0.9-draft
Contact: api@ninjarmm.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from tl_ninjarmm.models.custom_tab_label_public_api_dto import (
    CustomTabLabelPublicApiDTO,
)
from typing import Set
from typing_extensions import Self


class CustomTabSummaryPublicApiDTO(BaseModel):
    """
    CustomTabSummaryPublicApiDTO
    """  # noqa: E501

    labels: Optional[List[CustomTabLabelPublicApiDTO]] = Field(
        default=None,
        description="Each tab will be returned as a CustomTabLabelPublicApiDTO object. These will not be sorted. It is the callers responsibility to sort the tabs if that is desired",
    )
    role_hierarchy: Optional[List[StrictInt]] = Field(
        default=None,
        description="When retrieving tab summaries for roles, we will return the role hierarchy as a list of roleIds in order from the root node to the requested roleId. This is useful for sorting tabs, since that is done on a per-role basis",
        alias="roleHierarchy",
    )
    __properties: ClassVar[List[str]] = ["labels", "roleHierarchy"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CustomTabSummaryPublicApiDTO from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in labels (list)
        _items = []
        if self.labels:
            for _item_labels in self.labels:
                if _item_labels:
                    _items.append(_item_labels.to_dict())
            _dict["labels"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CustomTabSummaryPublicApiDTO from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "labels": [
                    CustomTabLabelPublicApiDTO.from_dict(_item)
                    for _item in obj["labels"]
                ]
                if obj.get("labels") is not None
                else None,
                "roleHierarchy": obj.get("roleHierarchy"),
            }
        )
        return _obj
