# coding: utf-8

"""
NinjaOne Public API 2.0

NinjaOne Public API documentation.

The version of the OpenAPI document: 2.0.9-draft
Contact: api@ninjarmm.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictInt,
    StrictStr,
    field_validator,
)
from typing import Any, ClassVar, Dict, List, Optional
from tl_ninjarmm.models.attribute_form_content_common_item_public_api_dto import (
    AttributeFormContentCommonItemPublicApiDTO,
)
from typing import Set
from typing_extensions import Self


class CustomTabPublicApiDTO(BaseModel):
    """
    CustomTabPublicApiDTO
    """  # noqa: E501

    default: Optional[StrictBool] = None
    id: Optional[StrictInt] = Field(default=None, description="Tab ID")
    name: Optional[StrictStr] = Field(default=None, description="Display name of tab")
    entity_type: Optional[StrictStr] = Field(
        default=None,
        description="The type of object the tab is for (ie, NODE_ROLE, ORGANIZATION, END_USER, etc)",
        alias="entityType",
    )
    entity_id: Optional[StrictInt] = Field(
        default=None,
        description="For entity types that are bound to an ID, this will be the id of the object. Otherwise, omitted",
        alias="entityId",
    )
    parent_tab_id: Optional[StrictInt] = Field(
        default=None,
        description="For tab extensions, this will be the id of the tab that was extended (ie, the 'root tab')",
        alias="parentTabId",
    )
    items: Optional[List[AttributeFormContentCommonItemPublicApiDTO]] = Field(
        default=None, description="The content of the tab"
    )
    position: Optional[StrictInt] = Field(
        default=None,
        description="Integer representing the position of the tab. Used when ordering multiple tabs",
    )
    is_default: Optional[StrictBool] = Field(
        default=None,
        description="True if this is a default tab, false otherwise",
        alias="isDefault",
    )
    __properties: ClassVar[List[str]] = [
        "default",
        "id",
        "name",
        "entityType",
        "entityId",
        "parentTabId",
        "items",
        "position",
        "isDefault",
    ]

    @field_validator("entity_type")
    def entity_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["NODE_ROLE", "ORGANIZATION", "END_USER"]):
            raise ValueError(
                "must be one of enum values ('NODE_ROLE', 'ORGANIZATION', 'END_USER')"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CustomTabPublicApiDTO from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in items (list)
        _items = []
        if self.items:
            for _item_items in self.items:
                if _item_items:
                    _items.append(_item_items.to_dict())
            _dict["items"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CustomTabPublicApiDTO from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "default": obj.get("default"),
                "id": obj.get("id"),
                "name": obj.get("name"),
                "entityType": obj.get("entityType"),
                "entityId": obj.get("entityId"),
                "parentTabId": obj.get("parentTabId"),
                "items": [
                    AttributeFormContentCommonItemPublicApiDTO.from_dict(_item)
                    for _item in obj["items"]
                ]
                if obj.get("items") is not None
                else None,
                "position": obj.get("position"),
                "isDefault": obj.get("isDefault"),
            }
        )
        return _obj
