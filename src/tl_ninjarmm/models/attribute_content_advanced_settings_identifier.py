# coding: utf-8

"""
NinjaOne Public API 2.0

NinjaOne Public API documentation.

The version of the OpenAPI document: 2.0.9-draft
Contact: api@ninjarmm.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictInt,
    StrictStr,
    field_validator,
)
from typing import Any, ClassVar, Dict, List, Optional
from typing import Set
from typing_extensions import Self


class AttributeContentAdvancedSettingsIdentifier(BaseModel):
    """
    AttributeContentAdvancedSettingsIdentifier
    """  # noqa: E501

    automatic_generation_enabled: Optional[StrictBool] = Field(
        default=None, alias="automaticGenerationEnabled"
    )
    scope: Optional[StrictStr] = None
    assign_to: Optional[StrictStr] = Field(default=None, alias="assignTo")
    type: Optional[StrictStr] = None
    next_sequence_number: Optional[StrictInt] = Field(
        default=None, alias="nextSequenceNumber"
    )
    prefix: Optional[StrictStr] = None
    suffix: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = [
        "automaticGenerationEnabled",
        "scope",
        "assignTo",
        "type",
        "nextSequenceNumber",
        "prefix",
        "suffix",
    ]

    @field_validator("scope")
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["NONE", "GLOBAL", "ORGANIZATION"]):
            raise ValueError(
                "must be one of enum values ('NONE', 'GLOBAL', 'ORGANIZATION')"
            )
        return value

    @field_validator("assign_to")
    def assign_to_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["NEW_ASSETS_ONLY", "NEW_AND_EXISTING_ASSETS"]):
            raise ValueError(
                "must be one of enum values ('NEW_ASSETS_ONLY', 'NEW_AND_EXISTING_ASSETS')"
            )
        return value

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["CUSTOM", "SEQUENCE"]):
            raise ValueError("must be one of enum values ('CUSTOM', 'SEQUENCE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AttributeContentAdvancedSettingsIdentifier from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AttributeContentAdvancedSettingsIdentifier from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "automaticGenerationEnabled": obj.get("automaticGenerationEnabled"),
                "scope": obj.get("scope"),
                "assignTo": obj.get("assignTo"),
                "type": obj.get("type"),
                "nextSequenceNumber": obj.get("nextSequenceNumber"),
                "prefix": obj.get("prefix"),
                "suffix": obj.get("suffix"),
            }
        )
        return _obj
